{
  "name": "sitio-comercial",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter @sitio/web dev",
    "build": "bun run --filter @sitio/web build",
    "start": "node apps/web/.output/server/index.mjs",
    "lint": "bun run --filter @sitio/web lint",
    "format": "bun run --filter @sitio/web format"
  },
  "engines": {
    "node": ">=20"
  }
}
